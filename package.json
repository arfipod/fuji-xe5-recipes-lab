{
  "name": "fuji-xe5-recipes-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first Fujifilm X-E5 recipe parser, editor, backup and USB laboratory.",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "verify": "npm run check && npm test && node scripts/design-check.mjs && node scripts/verify.mjs",
    "baseline:browser": "node --experimental-websocket scripts/browser-baseline.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
